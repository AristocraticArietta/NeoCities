<div class="shrines-nav-wrapper" x-data="{ toggleNav : false }">
    <button @click="toggleNav = !toggleNav" :aria-expanded="toggleNav" class="shrines-nav-button">Navigation</button>
    <nav class="shrines-nav" x-show="toggleNav">
        <a href="/" class="shrines-nav-link">Home</a>
        <a href="/fun-stuff/" class="shrines-nav-link">Fun Stuff</a>
        <a href="/fun-stuff/shrines" class="shrines-nav-link">Shrines</a>
    </nav>
</div>
<style>
    .shrines-nav-wrapper {
        position:fixed;
        top: 1em;
        left: 1em;
        z-index: 2;
        display: flex;
        flex-direction: column;
        align-items: end;
        justify-content: center;
        gap: var(--gap-regular);
    }
    .shrines-nav-button {
        border: var(--border-glass);
        border-radius: var(--radius-glass);
        background-color: var(--bg-glass);
        transition: transform 0.2s ease;
        font: inherit;
        font-weight: 600;
        font-size: 1em;
        padding: 0.5em;
        color: #ffffff;
    }
    .shrines-nav-button:hover {
        transform: scale(1.1);
    }
    .shrines-nav {
        border: var(--border-glass);
        border-radius: var(--radius-glass);
        background-color: var(--bg-glass);
        display: flex;
        flex-direction: row;
        align-items: center;
        justify-content: end;
    }
    .shrines-nav-link {
        transition: transform 0.2s ease;
        font: inherit;
        font-weight: 600;
        font-size: 1em;
        text-decoration: none;
		position: relative;
        overflow: hidden;
        color: inherit;
        padding: 0.5em;
    }
    .shrines-nav-link:before {
		content: "";
		position: absolute;
		left: 0;
		right: 0;
		bottom: 0;
		top: 0;
		background-color: var(--accent2);
        border-radius: var(--radius-glass);
		z-index: -1;
		transform: scaleY(0);
		transform-origin: bottom center;
		transition: transform 0.2s ease-out;
	}
    .shrines-nav-link:hover::before {
		transform: scaleY(1);
	}
</style>